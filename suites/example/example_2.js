'use strict';
var debug=require('debug')('RoambiTest:login_again:debug');

describe("login again", function doLogin() {
	it("should login again", function doTest(done) {
		debug("login again");
		done();
	});
	it("should login again again", function doTest(done) {
		debug("again");
		done();
	});
});