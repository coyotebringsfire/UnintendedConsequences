'use strict';
var debug=require('debug')('RoambiTest:login_again:debug');

describe("example 2", function doLogin() {
	it("should example again", function doTest(done) {
		debug("example again");
		done();
	});
	it("should example again again", function doTest(done) {
		debug("again");
		done();
	});
});